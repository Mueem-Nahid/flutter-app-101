import 'package:flutter/cupertino.dart';

class ListFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is List fragment'),
    );
  }
}