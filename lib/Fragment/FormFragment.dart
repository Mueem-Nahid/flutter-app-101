import 'package:flutter/cupertino.dart';

class FormFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('This is Form fragment'),
    );
  }
}